# Go binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
main

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Go workspace file
go.work

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Log files
*.log

# Kubernetes secrets (if any) — real secret material must never be committed;
# example/template manifests belong under k8s/ and must be clearly marked.
secrets/
*.secret.yaml
# Local-only test token consumed by k8s/overlays/kind's secretGenerator
# (see k8s/overlays/kind/kustomization.yaml) — regenerated by `make
# deploy-local`/`make validate`, never a real credential, never committed.
**/.secret.local

# Environment files
.env
.env.local
.env.*.local

# Local scratch state
.kind-kubeconfig
/bin/
