apiVersion: v1
kind: ServiceAccount
metadata:
  name: echo-pong
  namespace: echo-pong
  labels:
    app.kubernetes.io/name: echo-pong
# The app never talks to the Kubernetes API, so it doesn't need its token
# mounted at all — this removes a credential the pod has no use for.
automountServiceAccountToken: false
